#pragma once

#include <string>
#include <vector>

namespace Nectere
{
	namespace Dp
	{
		namespace StringUtils
		{
			bool Find(const std::string &, char, unsigned long &);
			bool Find(const std::string &, char);
		};
	}
}